services:
  app:
    build: .
    container_name: ba-getdataforskolekube
    volumes:
      - filer-istskole360:/shr/ISTSkole360
    env_file:
      - stack.env
    restart: on-failure
volumes:
  filer-istskole360:
    external: true